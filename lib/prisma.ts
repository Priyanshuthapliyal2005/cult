import { PrismaClient } from '@prisma/client';

export const prisma = new PrismaClient();

// The rest of your application code can go here, and you can use the `prisma` instance to interact with your database.